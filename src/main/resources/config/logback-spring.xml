<?xml version="1.0" encoding="UTF-8" ?>
<!--
    对于配置文件：如果配置文件 logback-test.xml 和 logback-spring.xml 都不存在，那么 logback 默认地会调用BasicConfigurator，
    创建一个最小化配置。最小化配置由一个关联到根 logger 的ConsoleAppender 组成。
    输出用模式为%d{HH:mm:ss.SSS} [%thread] %-5level %logger{200} - %msg%n 的 PatternLayoutEncoder 进行格式化，logger{200} 表示该字段压缩为 200个长度显示
    root logger 默认级别是 DEBUG。
    scan:当此属性设置为true时，配置文件如果发生改变，将会被重新加载，默认值为true。
    scanPeriod:设置监测配置文件是否有修改的时间间隔，如果没有给出时间单位，默认单位是毫秒。当scan为true时，此属性生效。默认的时间间隔为1分钟。
    debug:当此属性设置为true时，将打印出logback内部日志信息，实时查看logback运行状态。默认值为false。
-->
<configuration>

    <!-- 用来定义变量值的标签 -->
    <springProperty scope="context" name="APP_NAME" source="spring.application.name" defaultValue="MoLiApi"/>
    <springProperty scope="context" name="LOG_PATH" source="logging.file.path" defaultValue="log"/>
    <!-- 开发环境使用的控制台彩色日志模式 -->
    <property name="CONSOLE_LOG_PATTERN"
              value="[%X{traceId:-NONE},%X{spanId:-NONE},%X{requestId:-NONE}] %d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{80} - %msg%n"/>

    <!-- key: 标识此timestamp的名字; datePattern: 设置将当前时间转换为字符串的模式 -->
    <timestamp key="bySecond" datePattern="yyyy-MM-dd'T'HH:mm:ss.SSSZ"/>

    <!-- 每个logger都关联到logger上下文 -->
    <contextName>${APP_NAME}</contextName>

    <springProfile name="!container &amp; !json-console">
        <appender name="stdout" class="ch.qos.logback.core.ConsoleAppender">
            <encoder>
                <pattern>${CONSOLE_LOG_PATTERN}</pattern>
                <charset>UTF-8</charset>
            </encoder>
        </appender>
    </springProfile>

    <springProfile name="container | json-console">
        <appender name="stdout_json" class="ch.qos.logback.core.ConsoleAppender">
            <encoder class="org.springframework.boot.logging.logback.StructuredLogEncoder">
                <format>${CONSOLE_LOG_STRUCTURED_FORMAT}</format>
                <charset>${CONSOLE_LOG_CHARSET}</charset>
            </encoder>
        </appender>
    </springProfile>

    <!-- 容器环境只写标准输出；非容器环境保留滚动文件日志。 -->
    <springProfile name="file-log">
        <appender name="file" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <!-- 日志文件的路径及文件名 -->
        <File>${LOG_PATH}/${APP_NAME}.log</File>
        <!-- 如果是 true，日志被追加到文件结尾，如果是 false，清空现存文件，默认是true -->
        <append>true</append>
        <!-- 如果是 true，日志会被安全的写入文件，即使其他的FileAppender也在向此文件做写入操作，效率低，默认是 false -->
        <prudent>false</prudent>
        <!-- 滚动记录文件，先将日志记录到指定文件，当符合某个条件时，将日志记录到其他文件 -->
        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <!-- 历史日志文件的存放路径和名称 -->
            <fileNamePattern>${LOG_PATH}/log/%d{yyyy-MM-dd}.log.gz</fileNamePattern>
            <!-- 日志文件最大的保存历史 数量-->
            <maxHistory>30</maxHistory>
        </rollingPolicy>

        <!-- 使用 Spring Boot 原生结构化日志编码器输出 JSON -->
        <encoder class="org.springframework.boot.logging.logback.StructuredLogEncoder">
            <format>${FILE_LOG_STRUCTURED_FORMAT}</format>
            <charset>${FILE_LOG_CHARSET}</charset>
        </encoder>
        </appender>

        <appender name="debug" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <filter class="ch.qos.logback.classic.filter.LevelFilter">
            <level>ERROR</level>
            <onMatch>ACCEPT</onMatch>
            <onMismatch>DENY</onMismatch>
        </filter>
        <!-- 日志文件的路径及文件名 -->
        <File>${LOG_PATH}/${APP_NAME}_error.log</File>
        <append>true</append>
        <prudent>false</prudent>
        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <!-- 历史日志文件的存放路径和名称 -->
            <fileNamePattern>${LOG_PATH}/error/error_%d{yyyy-MM-dd}_debug.log.gz</fileNamePattern>
            <maxHistory>30</maxHistory>
        </rollingPolicy>

        <!-- 使用 Spring Boot 原生结构化日志编码器输出 JSON -->
        <encoder class="org.springframework.boot.logging.logback.StructuredLogEncoder">
            <format>${FILE_LOG_STRUCTURED_FORMAT}</format>
            <charset>${FILE_LOG_CHARSET}</charset>
        </encoder>
        </appender>
    </springProfile>


    <!--MyBatis log configure-->
    <logger name="java.sql.Connection" level="DEBUG"/>
    <logger name="java.sql.Statement" level="DEBUG"/>
    <logger name="java.sql.PreparedStatement" level="DEBUG"/>
    <logger name="java.sql.ResultSet" level="DEBUG"/>

    <!-- 组件的日志输出 -->
    <logger name="org.springframework" level="WARN"/>
    <logger name="org.apache.http" level="WARN"/>
    <logger name="sun.rmi" level="WARN"/>
    <logger name="com.zaxxer" level="WARN"/>
    <logger name="cn.hhj.system.listener.LoadConfigListener" level="WARN"/>
    <logger name="org.hibernate" level="WARN"/>
    <logger name="com.baomidou.mybatisplus.core" level="WARN"/>
    <logger name="org.mybatis.spring.mapper" level="WARN"/>
    <logger name="com.baomidou.mybatisplus.extension" level="WARN"/>

    <!-- 表示 >=info 级别的日志输出到控制台 (这里开发环境看到的就是彩色文本) -->
    <springProfile name="!container &amp; !json-console">
        <root level="info">
            <appender-ref ref="stdout"/>
        </root>
    </springProfile>

    <springProfile name="file-log">
        <logger name="com.bingchunmoli" level="debug" additivity="true">
            <appender-ref ref="file"/>
            <appender-ref ref="debug"/>
        </logger>
    </springProfile>

    <springProfile name="container | json-console">
        <root level="info">
            <appender-ref ref="stdout_json"/>
        </root>
    </springProfile>

</configuration>
